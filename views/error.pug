extends layout.pug
block content
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  h2= error.message
  h3= error.status
  p This page is powered by:
  img(src='../images/error.png')
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  p= error.stack
